Add a brief summary of changes since the last changelog entry to `CHANGELOG.md`.

Use `git log` to find what changed since the last entry, then summarize from the perspective of an agent that needs to update persistent-agent via the `update` skill. Focus on what the agent needs to know to resolve conflicts and adapt: renamed/moved files, changed interfaces, removed scripts, new conventions.

Keep entries terse. Group by date, newest first.
