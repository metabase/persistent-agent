The latest changes are in the `master` branch in `upstream`, if any, otherwise in `origin`.

Fetch changes, merge into the current branch, then fix conflicts if any.

During conflict resolution use CHANGELOG.md to understand the semantics of changes, and use that information to update the repository without losing any user data.
